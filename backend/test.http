### Test Health Endpoint (localhost only)
GET http://localhost:5001/health

### Note: This API is designed to run behind a reverse proxy
### Direct external access is blocked by design
### HTTPS should be configured at the reverse proxy level